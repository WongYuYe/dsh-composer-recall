# pokemon-claw GitHub 上传检查清单（2026-03-13）

## 已处理

- [x] 将 `frontend/.git`、`backend/.git` 移除，收为单仓库结构
- [x] 新增项目级 `.gitignore`
- [x] 将脚本中的绝对路径改为相对路径推导
- [x] 确认 `backend/.env` 不会被 Git 跟踪
- [x] 重新验证前端构建：`frontend npm run build`

## 上传前仍建议确认

- [ ] README 是否已调整为对外仓库首页说明，而不只是本地工作记录
- [ ] 是否补充 `LICENSE`
- [ ] 是否决定保留哪些 docs，避免把过多机器绑定信息直接公开
- [ ] 是否再跑一轮人工敏感信息检查（文档、脚本、配置示例）
- [ ] 是否整理一次首个公开版本 commit（可选）

## 当前结论

当前仓库已经从“明显不适合直接上传”提升到“基本可上传，但建议再补 README / LICENSE / 文档筛选后上传更稳”。
