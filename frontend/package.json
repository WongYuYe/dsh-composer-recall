{
  "name": "openclaw-show",
  "version": "1.0.0",
  "private": true,
  "description": "OpenClaw status pixel dashboard",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "node build.mjs",
    "start:prod": "OPENCLAW_STATIC_ROOT=dist node server.js"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.1"
  }
}
