window.OPENCLAW_CONFIG = {
  endpoint: "api/openclaw/status",
  wsEndpoint: "./ws/openclaw/status",
  taskStatsEndpoint: "api/tasks/stats",
  taskRuntimeEndpoint: "api/tasks/runtime",
  pollIntervalMs: 30000,
  syncingPollIntervalMs: 15000,
  offlinePollIntervalMs: 30000,
  minRefreshGapMs: 5000,
  requestTimeoutMs: 12000,
  wsReconnectDelayMs: 5000,
  wsHealthyWindowMs: 20000,
  hiddenTabPollMultiplier: 2,
  apiKey: "",
  authToken: "",
  headers: {},
};
